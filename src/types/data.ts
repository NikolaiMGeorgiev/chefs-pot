export type RegistrationData = {
    firstName: string, 
    lastName: string, 
    username: string, 
    email: string, 
    password: string
}

export type LoginData = {
    username: string, 
    password: string
}

export type ProfileData = {
    username: string, 
    email: string, 
}